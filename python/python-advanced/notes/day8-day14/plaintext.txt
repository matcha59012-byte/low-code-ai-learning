4. 函数调用语法：`函数名(参数1值, 参数2值)` | 廖雪峰Python教程-【定义函数】章节 |
| 2 | 函数的参数类型 | 25分钟 | 1. 必选参数：调用时必须传入的参数，数量和顺序必须和定义一致<br>   例：`def add(a, b): return a + b`，调用时必须传两个参数<br>2. 默认参数：给参数设置默认值，调用时可以不传，不传就用默认值<br>   例：`def add(a, b=10): return a + b`，调用`add(5)`时，b默认是10<br>   核心规则：默认参数必须放在参数列表的最后<br>3. 参数传递：调用函数时，把值传递给参数，函数内部的参数是局部变量，不会影响外部变量 | 廖雪峰Python教程-【函数的参数】章节 |
| 3 | 函数的返回值与文档注释 | 15分钟 | 1. `return`语句：函数执行完后，返回结果给调用者<br>   - 函数可以没有return，默认返回None<br>   - return会立即终止函数，后面的代码不会执行<br>   - 可以返回多个值，用逗号分隔，本质是返回一个元组<br>2. 文档字符串：函数定义的第一行，用三引号包裹，说明函数的用途、参数、返回值<br>   调用`help(函数名)`可以查看文档字符串，是专业开发的好习惯 | 廖雪峰Python教程对应章节 |

#### 当日实操任务
##### 必做任务（必须全部完成）
1.  创建文件 `code/python-advanced/day8_function.py`，完成以下练习：
 - 定义加法函数 `add(a, b)`，返回两个数的和，调用测试
 - 定义成绩评级函数 `grade_score(score)`，传入分数，返回评级（A/B/C/D），调用测试
 - 定义年薪计算函数 `calculate_annual_salary(monthly_salary, bonus=0)`，月薪是必选参数，年终奖是默认参数（默认0），返回年薪，调用测试（传年终奖和不传年终奖两种情况）
 - 给每个函数添加完整的文档字符串，说明用途、参数、返回值
2.  把第一周写的「猜数字游戏」「登录验证脚本」「优惠计算脚本」，分别封装成独立的函数，放在同一个文件里，调用执行
3.  所有代码添加详细注释，运行无报错，截图保存，提交到Git仓库

##### 选做任务（有余力完成）
1.  学习可变参数 `*args` 和关键字参数 `**kwargs`，理解其用法
2.  定义一个函数，返回多个值（比如返回用户的姓名、年龄、城市），调用并接收返回值

#### 当日验收红线
能独立定义和调用函数，正确设置必选参数和默认参数，用return返回结果，用函数封装重复代码，代码运行无语法报错。

#### 新手避坑&高频报错解决
1.  报错「NameError: name '函数名' is not defined」：
 - 原因：函数必须先定义、后调用，把调用代码写在了定义前面
 - 解决：把函数定义放在文件的前面，调用代码放在后面
2.  报错「TypeError: 函数名() missing 1 required positional argument」：
 - 原因：必选参数没有传够数量
 - 解决：检查函数定义的参数数量，调用时传够所有必选参数
3.  默认参数放在了前面报错：
 - 原因：默认参数必须放在参数列表的最后
 - 解决：调整参数顺序，必选参数在前，默认参数在后

---

## Day9 变量作用域、模块与第三方库安装
### 当日学习总目标
理解变量的作用域规则，掌握Python模块的导入与使用，能独立安装和使用第三方库，为后续API调用打基础。
#### 学习时长分配
知识点学习：60分钟 | 实操任务：90分钟 | 复盘笔记：30分钟

#### 核心必学知识点
| 知识点序号 | 学习目标 | 学习时长 | 详细学习内容 | 配套资源 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 变量作用域 | 20分钟 | 1. 什么是变量作用域？变量的有效范围，分为局部变量和全局变量<br>2. 局部变量：定义在函数内部的变量，只在函数内部有效，函数执行完就销毁<br>3. 全局变量：定义在函数外部的变量，整个文件都有效<br>4. 核心规则：<br>   - 函数内部可以读取全局变量，但不能直接修改全局变量<br>   - 如果要在函数内部修改全局变量，必须用`global`关键字声明<br>   - 函数内部的局部变量和全局变量同名时，优先使用局部变量 | 廖雪峰Python教程-【变量作用域】章节 |
| 2 | 模块与包的导入 | 25分钟 | 1. 什么是模块？一个.py文件就是一个模块，把代码拆分到不同文件里，方便管理和复用<br>2. 什么是包？包含多个.py文件和一个`__init__.py`文件的文件夹，用来组织模块<br>3. 导入语法：<br>   - 导入整个模块：`import 模块名`，调用时用`模块名.函数名()`<br>   - 从模块导入特定函数：`from 模块名 import 函数名1, 函数名2`，调用时直接用函数名<br>   - 从模块导入所有内容（不推荐）：`from 模块名 import *`<br>4. 常用内置模块：`math`（数学计算）、`random`（随机数）、`time`（时间） | 廖雪峰Python教程-【模块】章节 |
| 3 | 第三方库安装与使用 | 15分钟 | 1. 什么是第三方库？别人写好的、可以直接用的代码库，比如requests（API调用）、pandas（数据处理）<br>2. pip安装命令：<br>   - 安装最新版：`pip install 库名`<br>   - 安装指定版本：`pip install 库名==版本号`<br>   - 国内镜像源安装（速度快）：`pip install 库名 -i https://pypi.tuna.tsinghua.edu.cn/simple`<br>3. 验证安装：`pip show 库名`，能看到库的信息即安装成功<br>4. 导入第三方库：和导入内置模块一样，`import 库名` | 廖雪峰Python教程-【安装第三方模块】章节 |

#### 当日实操任务
##### 必做任务（必须全部完成）
1.  创建文件 `code/python-advanced/day9_module.py`，完成变量作用域练习：
 - 定义全局变量 `study_progress = 0.1`
 - 定义函数 `update_progress(new_progress)`，尝试直接修改全局变量，看会发生什么
 - 用`global`关键字修改函数，让它能正确修改全局变量，调用测试
 - 打印修改后的全局变量，验证是否成功
2.  完成模块练习：
 - 创建工具模块 `code/python-advanced/utils.py`，把Day8写的加法函数、成绩评级函数、年薪计算函数，全部放进去
 - 创建 `day9_test_module.py` 文件，从`utils.py`中导入函数，调用执行，验证功能正常
3.  完成第三方库安装练习：
 - 用pip安装`requests`库（国内镜像源，速度快）
 - 用`pip show requests`验证安装成功
 - 导入`requests`库，打印`requests.__version__`，查看版本号
4.  所有代码添加详细注释，运行无报错，截图保存，提交到Git仓库

##### 选做任务（有余力完成）
1.  学习使用`random`内置模块，重写猜数字游戏，用`random.randint(1,100)`生成随机数
2.  学习使用`time`内置模块，给代码添加计时功能

#### 当日验收红线
能理解变量作用域规则，正确使用`global`关键字，能创建和导入模块，能独立安装和导入第三方库，代码运行无报错。

#### 新手避坑&高频报错解决
1.  报错「UnboundLocalError: local variable 'xxx' referenced before assignment」：
 - 原因：在函数内部直接修改全局变量，没有用`global`声明
 - 解决：在函数内部第一行，用`global 变量名`声明全局变量
2.  报错「ModuleNotFoundError: No module named 'xxx'」：
 - 原因：模块名写错了，或者没有安装第三方库
 - 解决：检查模块名拼写，第三方库用pip安装一下
3.  导入模块时循环导入报错：
 - 原因：A模块导入B，B模块又导入A，形成循环
 - 解决：调整代码结构，把公共代码放到单独的模块里，避免循环导入

---

## Day10 异常处理与文件操作
### 当日学习总目标
掌握Python异常处理的完整语法，能避免程序崩溃，掌握文件的读写操作，能独立读写文本文件和JSON文件。
#### 学习时长分配
知识点学习：60分钟 | 实操任务：90分钟 | 复盘笔记：30分钟

#### 核心必学知识点
| 知识点序号 | 学习目标 | 学习时长 | 详细学习内容 | 配套资源 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 异常处理核心语法 | 25分钟 | 1. 什么是异常？程序运行时出现的错误，比如用户输入非数字、文件不存在、网络连接失败，不处理会导致程序崩溃<br>2. 异常处理完整语法：<br>   ```python
try:
    可能会出错的代码
except 异常类型1 as e:
    处理异常类型1的代码
except 异常类型2 as e:
    处理异常类型2的代码
except Exception as e:
    处理所有其他异常的代码
else:
    没有异常时执行的代码
finally:
    无论有没有异常，都会执行的代码